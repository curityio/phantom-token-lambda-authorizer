{
  "name": "phantom-token-lambda-authorizer",
  "version": "0.1.0",
  "description": "A Lambda Authorizer that handles the Phantom Token Pattern",
  "main": "index.js",
  "scripts": {
    "package": "npm run clean && npm install ; zip phantom-token-lambda-authorizer.zip -r *.js *.json .env node_modules/",
    "clean": "rm -rf node_modules && rm -f phantom-token-lambda-authorizer.zip"
  },
  "keywords": [
    "oauth",
    "oidc",
    "curity",
    "lambda",
    "authorizer",
    "phantom-token"
  ],
  "author": "Jonas Iggbom - Curity",
  "license": "Apache-2.0",
  "dependencies": {
    "dotenv": "^10.0.0",
    "https": "^1.0.0"
  }
}
